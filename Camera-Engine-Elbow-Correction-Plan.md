# خطة تصحيح زاوية الكوع — الإصلاح + طبقات الثقة والرصد
# Camera Engine — Elbow Angle Correction: Remediation & Observability Plan

| | |
|---|---|
| **Status** | `ROADMAP` — WP-21…WP-26 منفّذة كوداً؛ M-A / M-E بانتظار جهاز |
| **SSOT for** | — (ليست as-built) |
| **As-built** | [`ElbowAngleEstimator.kt`](kmp-app/core/training-engine/src/commonMain/kotlin/com/movit/core/training/geometry/ElbowAngleEstimator.kt) |
| **Verified** | 2026-07-25 |

> **المصدر**: مراجعة `ElbowAngleEstimator` الكاملة (2026-07-25) — الكود + المنطق + مسار الاستدعاء على المنصتين + الاختبارات + ملفات التمارين المستهلِكة.
> **العلاقة بالخطط السابقة**: تكمل [Camera-Engine-3D-Accuracy-Plan.md](Camera-Engine-3D-Accuracy-Plan.md) (WP-15…WP-20) — الترقيم يتابع من **WP-21**. تُغلق البند المؤجَّل **WP-18/§4 (القرار المُقاس M-C)** وتستوعبه في M-E.
> **مرجع الخلفية**: [elbow-solution-knowledge-base.md](Docs/99-Archive/Superseded/elbow-solution-knowledge-base.md) (جرد محايد للبدائل) · [track-E-geometry-math.md](Docs/04-Research/camera-engine-review/track-E-geometry-math.md) (E-04/E-05/E-08).

---

## 0. الملخص التنفيذي

### 0.1 الفكر الحاكم

1. **زاوية الكوع غير قابلة للرصد أحياناً — وهذه حقيقة رياضية لا عيب برمجي.** حين يحتوي مستوى الذراع محورَ الكاميرا، لا يوجد heuristic فوق نفس الـ landmarks يسترجع المعلومة. النظام الصحيح **يعرف متى لا يعرف**.
2. **لا رقم بلا وسم.** كل زاوية كوع تخرج من المحرك تحمل ثقة. `HOLD` يتوقف عن اختلاق قيمة لا يميّزها المحرك عن قياس حقيقي.
3. **الوقاية قبل التصحيح.** أرخص طريقة لتقليل الخطأ هي تقليل عدد الإطارات التي تنشأ فيها الحالة أصلاً — إرشاد وضع الكاميرا في طور الإعداد.
4. **إشارة هندسية حقيقية تسبق الثوابت المضبوطة يدوياً.** أطوال العظام ثابتة لكل مستخدم؛ هذا قيد فيزيائي يمكن قياسه، بخلاف `CORRECTION_SCALE = 0.55f`.
5. **الأرقام تقرر مصير الخوارزمية، لا الحدس.** لا تعميم لأي مسار جديد قبل بوابة M-E.

### 0.2 قرارات المالك (لا تحتاج نقاشاً)

| القرار | الأثر على الخطة |
|---|---|
| **إعادة معايرة عتبات التمارين ليست قيداً** | WP-21 حرّة في تغيير شكل المخرج جذرياً؛ لا حاجة لحفظ توافق رقمي مع السلوك الحالي. هذا يرفع جدوى WP-25 تحديداً |
| الطبقات الثلاث الإضافية (ثقة + إرشاد + إعادة بناء) **كلها مطلوبة** | WP-23 → WP-24 → WP-25 بهذا الترتيب الإلزامي |
| الهجين لا يُحذف | يتحول من "مُصحِّح يخرج رقماً" إلى "حَكَم يخرج رقماً + ثقة" |

### 0.3 جدول المشكلات المكتشفة

| ID | المشكلة | الخطورة | الحزمة |
|---|---|---|---|
| **EL-01** | فرع `STRAIGHT` يُقيَّم **قبل** بوابات العمق ويتجاهل `maxDz`/`ang3D`/`sideStrength` كلياً — والذراع المثنية المتجهة نحو العدسة تُسقَط قرب 180° فتُصنَّف "مستقيمة" وتُدفَع أقرب لـ 180 | 🔴 P0 | WP-21 |
| **EL-02** | الدفعة `+ (180 − ang2D) × 0.5` تضغط النطاق **(150,180] → (165,180]** وتهدم طبقات الجودة (pad 150 / normal 160 / perfect 165)، مع قفزة ~15° عند العتبة 150 | 🔴 P0 | WP-21 |
| **EL-03** | انقطاع رياضي عند `MID_DEPTH=0.40`: `corrPct` يقفز 0.125 → 0.25 (≈11.25° عند `ang2D=90`)؛ والسقوف تبتلع التدرّج فيصير النظام ثلاث ثوابت 0%/12.5%/25% | 🟠 P1 | WP-21 |
| **EL-04** | سائق التصحيح `sideStrength` مشتق من دوران **الجذع**، بينما سبب العطل عمق **الطرف**. المنظر الأمامي + ذراع نحو العدسة (أسوأ حالة) ⇒ `sideStrength=0` ⇒ التصحيح معطّل تماماً | 🟠 P1 | WP-21 |
| **EL-05** | `isLowConfidence` يُحسب داخل الفرع الأخير فقط ⇒ `maxDz=0.9` مع `ang3D ≤ ang2D+12` يمرّ عبر `TRUST_3D` بثقة كاملة | 🟠 P1 | WP-21 |
| **EL-06** | اتجاه التصحيح أحادي (`× (1 − corrPct)` نازل دائماً) بينما خطأ 2D ثنائي الاتجاه — والمستندات نفسها تصف `dzShare` كـ **ثقة** لا كـ **اتجاه** | 🟠 P1 | WP-21 / WP-23 |
| **EL-07** | بوابة الرؤية تقرأ رؤية **world** التي تُملأ بـ `1f` عند الغياب على المنصتين ⇒ قد لا تعمل أبداً؛ ويناقض مبدأ F2 المطبَّق في الـ assembler | 🟡 P2 | WP-22 |
| **EL-08** | `smUaDz/smFaDz` تبدأ من `0f` ⇒ ~9 إطارات (300ms@30fps · 900ms@10fps) عمق مُقدَّر أقل من الحقيقي بعد **كل** حدود تمرين | 🟡 P2 | WP-22 |
| **EL-09** | `OUTPUT_SMOOTH`/`DZ_SMOOTH_ALPHA` ثوابت لكل إطار بينما `AdaptiveThroughputController` يهبط 30→15→10 fps أثناء الجلسة ⇒ ثابت الزمن يتغيّر 133ms→400ms بلا قصد | 🟡 P2 | WP-22 |
| **EL-10** | `HOLD` يعيد قيمة مجمّدة لا يميّزها المحرك عن قياس حقيقي (`JointAngles` أرقام `Double?` بلا قناة ثقة) | 🟡 P2 | WP-23 |
| **EL-11** | `applyElbowCorrection` مُعرَّف ولم يُوصَّل: `true` في كل مسارات الإنتاج ⇒ المصحِّح وحالته يعملان في القرفصاء والطعنات | 🟢 P3 | WP-22 |
| **EL-12** | الاختبارات تغطي البنية (عزل/حالة/اختيار استراتيجية) ولا يوجد اختبار رقمي واحد للدقة أو للاستمرارية أو للرتابة أو للتسخين | 🟢 P3 | WP-22 |

---

## 1. WP-21 — تصحيح منطق الحَكَم (Arbiter Logic Correctness)

> **يغلق**: EL-01, EL-02, EL-03, EL-04, EL-05 · **الجهد**: يوم واحد · **المخاطرة**: متوسطة (يغيّر المخرج عمداً — مقبول بقرار المالك)

### 1.1 المبدأ

الفرع الوحيد الذي يتجاهل كل إشارات الثقة (`STRAIGHT`) هو أول فرع يُقيَّم. القاعدة الجديدة: **الثقة تُحسب مرة واحدة قبل اختيار أي فرع، وكل فرع يخضع لها.**

### 1.2 التغييرات

1. **إعادة الترتيب** — احسب `ang2D, ang3D, uaDz, faDz, maxDz, facingRatio, isLowConfidence` **قبل** `when`. يصبح `isLowConfidence = maxDz > HIGH_DEPTH` مستقلاً عن الفرع المختار (يغلق EL-05).

2. **`STRAIGHT` مشروط بالعمق ومتصل**:
   ```
   STRAIGHT_SNAP_START = 172.0   // بدل STRAIGHT_ARM_GATE = 150.0
   STRAIGHT_SNAP_FULL  = 180.0
   الشرط: maxDz < LOW_DEPTH && ang2D > STRAIGHT_SNAP_START
   t      = smoothstep(STRAIGHT_SNAP_START, STRAIGHT_SNAP_FULL, ang2D)
   output = ang2D + (180.0 - ang2D) * t
   ```
   خصائصه: متصل عند 172 (‏`t=0` ⇒ بلا قفزة)، رتيب، ويحتفظ بالغرض الأصلي (تعويض ضوضاء المعالم قرب الاستقامة الحقيقية) دون ضغط نطاق 30°. مبرر العتبة 172: الإسقاط المنظوري يحوّل الخطوط إلى خطوط ⇒ الذراع المستقيمة فعلاً تُسقَط 180° دائماً، والانحراف الوحيد المشروع هو ضوضاء المعالم (يغلق EL-01 + EL-02).

3. **`corrPct` دالة واحدة متصلة** — تُحذف الفروع الحسابية `MILD_DOWN`/`DEEP_DOWN`:
   ```
   depthRamp = ((maxDz - LOW_DEPTH) / (HIGH_DEPTH - LOW_DEPTH)).coerceIn(0f, 1f)
   corrPct   = MAX_CORRECTION * depthRamp * correctionWeight
   ```
   عند `maxDz=0.40` تعطي `0.25 × 0.556 = 0.139` بدل القفزة 0.125→0.25 (يغلق EL-03).
   **`ElbowCorrectionStrategy` يبقى كما هو كوسم تشخيصي فقط** (‏`MILD_DOWN` = `depthRamp < 0.5`) حفاظاً على واجهة `ElbowDiagnosticsAdapter` ومختبر التشخيص.

4. **سائق التصحيح = عمق الطرف، ودوران الجذع معامل ثانوي**:
   ```
   correctionWeight = 0.6f + 0.4f * sideStrength   // بدل sideStrength وحده
   ```
   بذلك لا يعود المنظر الأمامي يصفّر التصحيح في الحالة التي بُني لأجلها. الوزن الثانوي يبقى لأن `facingRatio` إشارة صحيحة على جودة world z، لكنه لم يعد بوابة قطع (يغلق EL-04).

5. **اتجاه التصحيح** — يبقى نازلاً في WP-21 (تغييره يحتاج إشارة اتجاه حقيقية لا تتوفر قبل WP-25)، لكن **يُوثَّق صراحة في KDoc** أنه انحياز مقصود لا استنتاج، ويُسجَّل في تقرير الجلسة عبر WP-23 (يخفّف EL-06؛ الإغلاق النهائي في WP-25).

### 1.3 الملفات
`ElbowAngleEstimator.kt` (الثوابت `:11-28`، `estimateSide` `:86-200`) · `ElbowCorrectionDiagnostics.kt` (توثيق دلالة الوسم الجديدة)

### 1.4 معيار القبول
- لا قفزة > **1°** في المخرج لتغيّر 0.01° في `ang2D` عند أي حد فرع (اختبار مسح على 0…180 بخطوة 0.01).
- لا قفزة > **1°** لتغيّر 0.001 في `maxDz` عند 0.15 / 0.40 / 0.60.
- الرتابة: `ang2D₁ < ang2D₂` بنفس بقية المدخلات ⇒ `out₁ ≤ out₂`.
- سيناريو "ذراع 90° نحو العدسة، منظر أمامي": الاستراتيجية لم تعد `STRAIGHT`، و`isLowConfidence = true`.

---

## 2. WP-22 — نظافة البوابات والحالة والاختبارات

> **يغلق**: EL-07, EL-08, EL-09, EL-11, EL-12 · **الجهد**: يوم واحد · **المخاطرة**: منخفضة

### 2.1 التغييرات

1. **بوابة الرؤية → normalized** (`ElbowAngleEstimator.kt:99`): استبدل `ws/we/ww.isVisible()` بفحص `ns/ne/nw.isVisible(VisibilityDefaults.ANGLE_GATE)` — اتساق مع مبدأ F2 في [`PoseFrameAssembler.kt:230-232`](kmp-app/core/training-engine/src/commonMain/kotlin/com/movit/core/training/geometry/PoseFrameAssembler.kt:230) ومع مسار `calculateAngles` نفسه (يزيل خطر اختلاف مسار المصحِّح عن مسار الـ fallback `?: angles.leftElbow`).
   **قبل الحذف**: سجّل قيمة world visibility الفعلية إطاراً في `TrainingPipelineDiagnostics` لعيّنة من الجلسات — هذا يغلق بوابة **M-A** المعلّقة من WP-17 بدل تركها مفتوحة إلى الأبد.

2. **تهيئة EMA بأول عيّنة** بدل الصفر:
   ```kotlin
   if (smUaDz[side].isNaN()) smUaDz[side] = uaDzRaw else smUaDz[side] += a * (uaDzRaw - smUaDz[side])
   ```
   مع تغيير القيمة الابتدائية في `reset()` إلى `Float.NaN` (يغلق EL-08).

3. **ثوابت زمنية بدل ثوابت لكل إطار**:
   ```
   alpha = 1f - exp(-dt / tau)      dt = (ts - lastTs) مقيّدة إلى [1ms, 200ms]
   TAU_DZ_MS  = 250     (يعادل 0.18 عند 30fps)
   TAU_OUT_MS = 130     (يعادل 0.25 عند 30fps)
   ```
   يجعل السلوك ثابتاً عبر بروفايلات 30/15/10 fps والنزول التكيّفي أثناء الجلسة (يغلق EL-09).

4. **توصيل `applyElbowCorrection`**: يُشتق من `trackedJoints` في config التمرين ويُمرَّر عبر [`TrainingThroughputProfiles.toCameraConfiguration`](kmp-app/core/training-engine/src/commonMain/kotlin/com/movit/core/training/boundary/TrainingThroughputProfile.kt:69) → `resolveTrainingCameraConfiguration(useFrontCamera, elbowTracked)`. مواقع التحديث: `TrainingThroughputFlags.kt`, `TrainingSessionCameraHost.{android,ios}.kt` (يغلق EL-11).

5. **اختبارات جديدة** في `ElbowAngleEstimatorTest` (يغلق EL-12):
   | الاختبار | ما يحميه |
   |---|---|
   | `output_isContinuous_acrossAllBranchBoundaries` | EL-02, EL-03 |
   | `output_isMonotonicIn2DAngle` | EL-02 |
   | `output_isFrameRateInvariant_10fps_vs_30fps` | EL-09 |
   | `dzEma_convergesOnFirstFrame_afterReset` | EL-08 |
   | `frontalView_deepForeshortening_marksLowConfidence` | EL-01, EL-04, EL-05 |
   | `invisibleNormalizedLandmarks_holdOrNull` | EL-07 |

---

## 3. WP-23 — طبقة الثقة (الطبقة الممكِّنة)

> **يغلق**: EL-10 + يخفّف EL-06 · **الجهد**: 2–3 أيام · **المخاطرة**: متوسطة (تمس عقد `PoseFrame` والمحرك)
> **تبعية**: WP-21 و WP-22 منفَّذتان أولاً · **تُمكِّن**: WP-24 و WP-25 معاً

### 3.1 المبدأ
الرقم الذي يشغّل إرشاد الكاميرا (WP-24) هو نفسه الرقم الذي يُحكِّم بين الهجين وإعادة البناء (WP-25). بدون هذه الطبقة تبقى الطبقتان بلا مُشغِّل وبلا مِرجَّح — وهو بالضبط سبب فشل `ElbowAngleEstimator v1` تاريخياً (أنتج قيمة بلا وسيلة لمعرفة متى لا يُصدَّق).

### 3.2 النموذج (تغيير غير كاسر)
`JointAngles` تبقى كما هي. تُضاف إلى [`PoseFrame`](kmp-app/core/training-engine/src/commonMain/kotlin/com/movit/core/training/model/PoseFrame.kt):
```kotlin
val angleConfidence: Map<String, Float> = emptyMap()   // jointCode → [0,1]
```
> ⚠ **إلزامي**: `PoseFrame.mirrored()` يبدّل أكواد الزوايا يمين/يسار — يجب تبديل **مفاتيح** هذه الخريطة أيضاً عبر `PoseLandmarkMirroring.mirrorJointCode`، وإلا انفصلت الثقة عن الزاوية في الكاميرا الأمامية.

### 3.3 صيغة الثقة للكوع
حاصل ضرب عوامل مستقلة، كلها محسوبة أصلاً داخل المصحِّح:
```
visibilityFactor  = min(vis_shoulder, vis_elbow, vis_wrist)  مطبَّعة على ANGLE_GATE
depthFactor       = 1 - depthRamp                            (كلما زاد العمق قلّت الثقة)
agreementFactor   = 1 - clamp(|ang3D - ang2D| / 60°, 0, 1)   (تباعد المصدرين = شك)
holdFactor        = 1 - (ts - lastStableTs) / HOLD_TIMEOUT_MS (عند HOLD فقط، وإلا 1)
confidence        = visibilityFactor * depthFactor * agreementFactor * holdFactor
```
`HOLD` يبقى يعيد قيمة، لكن بثقة **تتناقص خطياً حتى 0** عند 500ms — فيتوقف عن كونه كذبة صامتة.

### 3.4 المستهلكون
| المستهلك | السلوك تحت `ANGLE_CONFIDENCE_MIN` |
|---|---|
| `JointEvaluator` | لا تُمنح درجة جودة (لا perfect/normal/pad) — القناة "غير مُقيَّمة" |
| `PhaseStateMachine` / عدّ التكرارات | لا يُغلق تكرار على إطار منخفض الثقة؛ الطور يُجمَّد لا يُلغى |
| تقرير الجلسة | نسبة الإطارات منخفضة الثقة لكل قناة — مدخل مباشر لـ WP-24 و M-E |
| مختبر التشخيص | عرض الثقة بجانب `strategy` |

عتبة واحدة في object سياسات (نمط `VisibilityDefaults`): `ANGLE_CONFIDENCE_MIN = 0.45f` (قيمة ابتدائية تُضبط في M-E).

### 3.5 الملفات
`PoseFrame.kt` · `ElbowAngleEstimator.kt` · `PoseFrameAssembler.kt` · `JointEvaluator.kt` · `PhaseStateMachine.kt` · `MovitTrainingEngine.kt` · `PoseLandmarkMirroring.kt` · `TrainingDebugModels.kt` + `ElbowDiagnosticsAdapter.kt`

### 3.6 معيار القبول
- إطار `HOLD` عمره 400ms ⇒ `confidence < 0.2` ولا يُغلق تكراراً.
- اختبار: `mirrored()` يبدّل مفاتيح `angleConfidence` بالتوازي مع الزوايا.
- تقرير جلسة كامل يحتوي نسبة الإطارات منخفضة الثقة لكل كوع.

---

## 4. WP-24 — إرشاد هندسة الالتقاط (الوقاية)

> **الجهد**: 2 أيام · **المخاطرة**: منخفضة (لا مساس برياضيات الزوايا) · **تبعية**: WP-23

### 4.1 المبدأ
البنية موجودة ~80%: [`CameraPositionDetector`](kmp-app/core/training-engine/src/commonMain/kotlin/com/movit/core/training/position/CameraPositionDetector.kt) (اتجاه الكاميرا مع hysteresis) و`PoseSceneDetector` و`PoseSceneExpectation` و`PositionValidator`. الناقص **إشارة واحدة خاصة بالكوع**.

### 4.2 الإشارة الجديدة — `armPlaneObservability`
مستوى الذراع يحدده متجها العضد والساعد؛ الرصد يكون جيداً حين يكون المستوى موازياً لمستوى الصورة، أي حين يكون عمودي المستوى موازياً لمحور البصر:
```
n = ua × fa            (حاصل ضرب اتجاهي من world landmarks)
observability = |n_z| / ‖n‖        ∈ [0,1]
```
`observability → 0` تعني أن مستوى الذراع يحتوي محور الكاميرا ⇒ الزاوية غير قابلة للرصد مهما فعلت الخوارزمية.
> **ملاحظة**: يُحسب من world z المشكوك فيه، فهو مؤشر **خشن** يكفي للإرشاد لا للقياس. عند تفعيل WP-25 يُستبدل بنسبة الاختصار المُقاسة (‏`l/l_max`) وهي إشارة أنظف لا تعتمد على z إطلاقاً.

### 4.3 التوصيل
1. تجميع زمني عبر `StableCameraDetector` الموجود (لا قرار من إطار واحد).
2. **العرض في طور الإعداد/العد التنازلي فقط** — رسالة واحدة عبر `PositionMessageResolver` ("حرّك الكاميرا جانباً قليلاً"). **ممنوع** المقاطعة أثناء التكرارات.
3. مُشغِّل ثانٍ بعد انتهاء المجموعة: إن تجاوزت نسبة الإطارات منخفضة الثقة (من WP-23) عتبة، اعرض تلميحاً لوضع الكاميرا للمجموعة التالية.
4. **مرحلة 2 (اختيارية)**: حقل اختياري في Exercise JSON للاتجاه الموصى به لكل تمرين — توسيع عقد، يُؤجَّل حتى تثبت المرحلة 1 قيمتها.

### 4.4 معيار القبول
- في تسجيل "ذراع نحو العدسة": `observability < 0.3` ويظهر الإرشاد في الإعداد.
- في تسجيل جانبي سليم: لا يظهر إرشاد (لا إنذارات كاذبة) — يُقاس على تسجيلات M-E.

---

## 5. WP-25 — إعادة بناء z بقيد أطوال العظام (خلف علم)

> **يغلق نهائياً**: EL-06 · **الجهد**: 4–6 أيام · **المخاطرة**: عالية · **تبعية**: WP-23 إلزامية

### 5.1 المبدأ
أطوال العظام ثابتة لكل مستخدم — قيد فيزيائي حقيقي يحل محل الثوابت المضبوطة يدوياً. تحت إسقاط شبه-متعامد، القطعة التي طولها الحقيقي المُسقَط الأقصى `l_max` وتظهر بطول `l`:
```
|dz| = l_max · √(1 − (l / l_max)²)
```

### 5.2 التفاصيل التنفيذية
1. **التطبيع للمسافة**: كل الأطوال 2D تُقسَّم على `sRef` = عرض الكتفين 2D (بعد `aspectYScale`) — يحيّد بُعد المستخدم عن الكاميرا وحجمه.
2. **تقدير `l_max`**: مئين 95 متحرك على نافذة ~10 ثوانٍ لكل قطعة (عضد/ساعد) لكل جانب. يُصفَّر مع `resetAngleTracking()`.
3. **غموض الإشارة**: فرضيتان (‏dz متوافقة / متعاكسة) ⇒ زاويتان مرشحتان. الترجيح بـ: الاتصال الزمني مع القيمة السابقة (الأقوى) + إشارة world z كـ prior ضعيف + رفض ما ينتهك القيد التشريحي للكوع.
4. **الثقة الذاتية** (المكسب المزدوج): حين `l / l_max → 0` تنفجر حساسية `d(angle)/d(l)` ⇒ ثقة منخفضة **مشتقة رياضياً** لا مضبوطة يدوياً. تُغذّى مباشرة إلى WP-23 وتحلّ محل `depthFactor` التقريبي.
5. **الدور في المسار**: مرشّح ثالث يدخل الحَكَم — **لا** بديل يستولي على المسار. علم `elbowReconstruction` افتراضياً `false`.

### 5.3 المخاطر المعروفة
| المخاطرة | التخفيف |
|---|---|
| انجراف `l_max` عند تغيّر المسافة/الملابس | نافذة متحركة + تطبيع بعرض الكتفين + تصفير عند حدود التمرين |
| `l_max` غير ناضج في أول ثوانٍ | ثقة = 0 حتى نضج العيّنة (لا مخرج قبل ذلك) |
| تكرار فشل v1 (انجراف الحالة) | الفارق الجوهري: v1 أنتج قيمة بلا ثقة؛ هنا الثقة شرط دخول (WP-23 تبعية إلزامية) |

### 5.4 معيار القبول
- على تسجيلات M-E بزوايا معلومة: **الاتساق عبر مواضع الكاميرا** (انحراف معياري لنفس الوضعية عبر 4 مواضع) أفضل من الهجين المُصلَح.
- لا تدهور في MAE عند المنظر الجانبي السليم.
- لا يُعمَّم إلا بعد اجتياز بوابة WP-26.

---

## 6. WP-26 — بوابة القياس M-E (تحل محل M-C)

> **الجهد**: 2 أيام تحضير + جلسات تسجيل · **يفتح**: قرار تعميم WP-25 وضبط `ANGLE_CONFIDENCE_MIN`

### 6.1 بروتوكول التسجيل
زوايا معلومة بالمنقلة (30 / 60 / 90 / 120 / 150 / 180) × 4 مواضع كاميرا (أمامي / أمامي-جانبي / جانبي / أمامي-خلفي) × ≥3 مستخدمين، إضافة إلى تمرينين ديناميكيين (bicep curl و push-up) بكل موضع.

### 6.2 المتنافسون
`ang2D` خام · `ang3D` خام · الهجين الحالي (قبل WP-21) · الهجين المُصلَح (بعد WP-21/22) · إعادة البناء (WP-25).

### 6.3 المقاييس
| المقياس | الوزن |
|---|---|
| **الاتساق عبر مواضع الكاميرا** (انحراف معياري لنفس الوضعية) | **الأهم** — هو تعريف المشكلة نفسها |
| MAE و P95 مقابل الزاوية المعلومة | عالٍ |
| أقصى خطأ | متوسط |
| نسبة الإطارات منخفضة الثقة (معايرة العتبة) | متوسط |

### 6.4 بوابة القرار
تُعمَّم إعادة البناء **فقط** إذا حسّنت الاتساق عبر الكاميرات دون تدهور MAE في المنظر الجانبي السليم. خلاف ذلك تبقى خلف العلم ويبقى الهجين المُصلَح هو الإنتاج.

---

## 7. الترتيب والتبعيات

```
WP-21 (منطق الحَكَم) ──┐
                        ├─→ WP-23 (الثقة) ──┬─→ WP-24 (إرشاد الكاميرا)
WP-22 (البوابات/الحالة)─┘                    └─→ WP-25 (إعادة البناء، خلف علم)
                                                        │
                                          WP-26 (M-E) ──┘ يقرر التعميم
```

| الأسبوع | العمل |
|---|---|
| 1 | WP-21 + WP-22 (متتاليتان، يومان) — مكسب فوري بلا بنية جديدة |
| 2 | WP-23 — الطبقة الممكِّنة |
| 3 | WP-24 (متوازية مع بداية WP-25) |
| 3–4 | WP-25 خلف علم |
| 4 | WP-26 — التسجيلات والقرار |

**قاعدتان غير قابلتين للكسر**: WP-23 قبل WP-24 و WP-25 · WP-25 لا تُعمَّم قبل WP-26.

---

## 8. المخاطر العامة

| المخاطرة | التخفيف | التراجع |
|---|---|---|
| WP-21 يغيّر عدّ التكرارات | مقبول بقرار المالك (المعايرة ليست قيداً)؛ يُوثَّق الفرق في M-E | ثوابت الفروع القديمة محفوظة في git |
| WP-23 يمس عقد `PoseFrame` والمحرك | حقل بقيمة افتراضية `emptyMap()` ⇒ كل المستهلكين الحاليين يعملون بلا تعديل | عتبة `ANGLE_CONFIDENCE_MIN = 0f` تُعطّل السلوك الجديد بسطر |
| WP-24 يزعج المستخدم بإنذارات كاذبة | الإعداد فقط + تجميع زمني + قياس معدل الإنذار الكاذب في M-E | علم عرض واحد |
| WP-25 يكرر فشل v1 | الثقة شرط دخول لا مخرج اختياري؛ خلف علم؛ بوابة M-E | العلم `false` |
| نسيان تبديل مفاتيح `angleConfidence` في `mirrored()` | اختبار إلزامي في معيار قبول WP-23 | — |

---

## 9. سجل التنفيذ

| الحزمة | الحالة | المنفّذ | التاريخ |
|---|---|---|---|
| WP-21 | ✅ مكتمل | Opus 5 | 2026-07-25 |
| WP-22 | ✅ مكتمل | Opus 5 | 2026-07-25 |
| WP-23 | ✅ مكتمل | Opus 5 | 2026-07-25 |
| WP-24 | ✅ مكتمل | Opus 5 | 2026-07-25 |
| WP-25 | ✅ مكتمل (خلف علم، افتراضي `false`) | Opus 5 | 2026-07-25 |
| WP-26 | ✅ الأداة جاهزة — التسجيلات بانتظار جهاز | Opus 5 | 2026-07-25 |

### قرارات تصميمية اتُّخذت أثناء التنفيذ (تعدّل الخطة)

1. **`STRAIGHT` صار يكبح `trust3D` أيضاً.** أثناء التنفيذ تبيّن أن الصيغة المخطّطة تجعل اللقطة المستقيمة **ميتة عملياً**: قرب 180° لا يمكن أن يتجاوز `ang3D − ang2D` قيمة صغيرة (لأن `ang3D ≤ 180`)، فيبقى وزن 3D كاملاً ويبتلع مسار 2D. المبرر الهندسي للكبح: الذراع المستقيمة فعلاً تُسقَط 180° دائماً (الإسقاط يحوّل الخطوط إلى خطوط)، بينما ضوضاء z **تثني** الثلاثية المستقيمة حتماً — أي أن الإسقاط هو المقدّر الأفضل في هذا النطاق، لا العكس. الصيغة النهائية: `trust3D = (1 − smoothstep(6°,18°, inflation)) × (1 − straightT)`.

2. **حدّان للثقة بدل حدّ واحد.** الخطة ذكرت عتبة واحدة؛ التنفيذ فصلها: `SCORING_MIN = 0.45` توقف **التقييم** فقط، و`PHASE_FREEZE_MIN = 0.25` توقف **آلة الأطوار**. السبب: تجميد الطور على عتبة 0.45 كان سيوقف العدّ في جلسات كاملة بزاوية كاميرا متوسطة — عقوبة أقسى من المشكلة.

3. **الثقة لا تدخل `skippedForFrame`.** إضافتها هناك كانت ستمسح ذاكرة `AngleSmoother` في كل إطار منخفض الثقة (‏`clearJoints`) فتُحدث تقطيعاً. المفاصل غير الموثوقة تُستبعد من التقييم والتغذية الراجعة فقط، وتاريخ التنعيم يبقى متصلاً.

4. **إحصاءات الثقة تُعرَض عبر `MovitTrainingEngine.angleConfidenceReport()`** لا عبر عقد تقرير الجلسة في الـ backend — نفس المعلومة لمستهلكي WP-24 و M-E بلا تغيير عقد مخزَّن.

5. **`ElbowCorrectionStrategy` بقيت كوسم تشخيصي فقط** (‏`MILD_DOWN`/`DEEP_DOWN` لم تعد فروعاً حسابية)، حفاظاً على واجهة `ElbowDiagnosticsAdapter` ومختبر التشخيص.

### ما نُفِّذ فعلياً

| البند | الملفات |
|---|---|
| حَكَم متصل + وسوم تشخيصية | `ElbowAngleEstimator.kt`, `ElbowCorrectionDiagnostics.kt` |
| سياسة الثقة + المراقب | `AngleConfidenceDefaults.kt`, `AngleConfidenceMonitor.kt` |
| قناة الثقة والرصد في الإطار | `PoseFrame.kt`, `PoseFrameAssembler.kt`, `PoseLandmarkMirroring.kt` |
| تجميد الطور وحجب التقييم | `FramePipelineExecutor.kt`, `MovitTrainingEngine.kt` |
| إرشاد وضع الكاميرا | `CameraPlacementAdvisor.kt`, `TrainingSessionViewModel.kt`, `strings.xml` (+`values-ar`) |
| إعادة بناء z خلف علم | `ElbowDepthReconstructor.kt`, `CameraSourceConfiguration.kt`, مصدرا الكاميرا |
| توصيل `elbowTracked` | `CameraFrameSource` (+actuals), `TrainingSessionCameraHost.*`, `TrainingSessionViewModel.kt` |
| بوابة M-A (رؤية world) | `TrainingPipelineDiagnostics.kt`, `PoseFrameAssembler.kt` |
| أداة M-E | `lab/ElbowMethodComparator.kt`, `lab/ElbowLabRecordingCodec.kt` |

**الاختبارات**: 301 اختباراً في `core:training-engine` (‏+30 جديداً: استمرارية، رتابة، ثبات معدل الإطارات، تسخين EMA، منظر أمامي منخفض الثقة، تبديل مفاتيح الثقة عند المرآة، سلوك المراقب والمرشد، إعادة البناء) و27 في `feature:training-debug`.

### تغطية وضع الـ Debug

| المسار | الخوارزمية (WP-21/22/25) | لوحة الكوع (ثقة + رصد) |
|---|---|---|
| `TrainingDebugAnalyzer` (صورة / فيديو / إعادة تجميع) | ✅ تلقائياً — نفس `ElbowAngleEstimator` | ✅ `collectElbowDiagnostics = true` في `TrainingDebugFrameFactory` |
| كاميرا Debug حيّة — Android | ✅ | ✅ `AndroidDebugCameraPoseSource` يمرّر `collectElbowDiagnostics = true` |
| كاميرا Debug حيّة — iOS | ✅ | ✅ منفذ تشخيص موصول عبر `IosCameraFrameSource.currentElbowEstimator()` |
| تصدير نصي / JSON | — | ✅ `elbowConfidence` و`elbowObservability` |

> **ثغرة سابقة أُغلقت أثناء هذه المراجعة**: مسارا الكاميرا الحيّة في الـ Debug كانا يعملان بـ `collectElbowDiagnostics = false`، ومسار iOS لم يكن يوصّل منفذ التشخيص أصلاً (‏`NoOp`) — أي أن **لوحة الكوع كانت فارغة دائماً في وضع الكاميرا الحيّة على المنصتين** قبل هذا العمل، بصرف النظر عن الخوارزمية.

**ما لا يشمله الـ Debug عمداً**: بوابة الثقة (تعليق التقييم / تجميد الطور) وإرشاد الكاميرا يعيشان في `MovitTrainingEngine` و`TrainingSessionViewModel`، ومختبر التشخيص لا يشغّل المحرك — فهو يعرض القياس الخام وثقته دون حجب، وهذا هو المطلوب من أداة تشخيص.

### مفتوح

- **M-E نفسها**: الأداة جاهزة والتسجيلات المرجعية تحتاج جهازاً — هي التي تقرر تعميم WP-25 وضبط `SCORING_MIN`/`PHASE_FREEZE_MIN`.
- **M-A**: العدّاد يعمل؛ يحتاج جلسة على جهاز لقراءة `worldVis constant1=` من سطر التشخيص الدوري.
- **iOS لم يُصرَّف** (المضيف Windows؛ Kotlin/Native لا يبني أهداف iOS هنا). تعديلات iOS مطابقة بنيوياً لنظيرتها على Android التي صُرِّفت بنجاح.
- **اختبار أحمر سابق لهذا العمل**: `AngleDiagnosticsBuilderTest.buildAll_includesPipelineSourceLabel` — تركيبة اختبار قديمة (كل المعالم في نقطة واحدة ⇒ `xyzAngle = null` بعد إصلاح F6)، خارج نطاق هذه الخطة.
