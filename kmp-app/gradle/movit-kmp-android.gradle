// Applied when movitKmp.namespace is set â€” configures kotlin.android for AGP KMP library plugin.
kotlin {
    android {
        namespace = movitAndroidNamespace
        compileSdk = movitAndroidCompileSdk
        minSdk = movitAndroidMinSdk
        androidResources.enable = true
        withHostTest { }
    }
}

tasks.register("testDebugUnitTest") {
    group = "verification"
    description = "Alias for testAndroidHostTest (KMP Android host unit tests)."
    dependsOn(tasks.named("testAndroidHostTest"))
}
