/**
 * Upload Types
 * ============
 *
 * Types for file upload categories used by the admin dashboard.
 * Upload logic is handled by the backend API.
 */

export type UploadCategory = 'camera-position-image' | 'exercise-image' | 'exercise-audio';
