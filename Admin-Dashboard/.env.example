# Backend API (NestJS) - required for API proxy
BACKEND_URL=http://localhost:4000

# Admin JWT - must match backend ADMIN_JWT_SECRET for auth to work
ADMIN_JWT_SECRET=change-me

# Optional: GCS bucket for relative exercise audio paths (defaults to legacy infra bucket `waytofix`)
# NEXT_PUBLIC_GCS_BUCKET_NAME=waytofix
