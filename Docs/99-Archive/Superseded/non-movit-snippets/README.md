# Non-Movit snippets

كود موروث من مشاريع أخرى (Laravel orders/inventory) — **ليس جزءاً من Movit**.

| الملف | المصدر المحتمل |
|-------|----------------|
| `GetPurchaseList.php` | Laravel `Modules\Orders` |
| `purchase_list.graphql` | GraphQL purchase list API |

**مُعلَّم للحذف** عند تنظيف الأرشيف — لا يُستخدم في `kmp-app` أو `backend`.
